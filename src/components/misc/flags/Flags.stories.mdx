import Text from '../../misc/text';
import Box from '../../layout/box';
import Flags from './index.js';
import { FlagMatrix } from './samples/flag-matrix';

import { Story, Meta, Preview, Props, Link } from '@storybook/addon-docs/blocks';


<Meta title="components/Misc" component={Flags} />

# Flags
<hr />

These flags comes from a sprite sheet bundled with this library, to make use of it, use the
`Flag` component and pass it its code. The contry codes can be found in this [link](https://www.nationsonline.org/oneworld/country_code_list.htm) use
the code is always apha 2 coding.

<Props of={Flags} />

<Preview>
  <Story name="Flags">
    <FlagMatrix />
  </Story>
</Preview>

