import Box from "../../layout/box";
import Tooltip from "./index.js";
import Button from '../button';

import {
  Story,
  Meta,
  Preview,
  Props,
  Link
} from "@storybook/addon-docs/blocks";

<Meta title="Misc/Tooltip" component={Tooltip} />

# Tooltip
<hr />

A small tooltip icon that shows messages both above or below when hovered with the mouse.


### Top
<Preview withSource="open">
  <Story name="Top">
    <Box horizontal>
      <Tooltip>Some short text</Tooltip>
      <Tooltip size={24}>A medium text that's just as meaningul</Tooltip>
      <Tooltip size={32} color="#ff0057">A pretty large text that details relevant information about a certain topic or on-screen component</Tooltip>
    </Box>
  </Story>
</Preview>

### Bottom placement
<Preview withSource="open">
  <Story name="Bottom">
    <Box horizontal>
      <Tooltip bottom size={32} color="#ff0057">Some short text</Tooltip>
      <Tooltip bottom size={24} >A medium text that's just as meaningul</Tooltip>
      <Tooltip bottom>A pretty large text that details relevant information about a certain topic or on-screen component</Tooltip>
    </Box>
  </Story>
</Preview>



<Props of={Tooltip} />
