import styled from 'styled-components';

const StyledButton = styled.button`
  font-size: 0.75rem;
  font-weight: bold;
  border-radius: 4px;
  min-width: 3rem;
  min-height: 1.4rem;
  padding: 0.4rem 1rem;
  cursor: pointer;
  transition: all 0.2s ease-in;
`;

export { StyledButton };
