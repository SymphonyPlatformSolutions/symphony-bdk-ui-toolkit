import { DynamicTabsSample } from './samples/dynamic-demo';
import DynamicTabs from '.'
import Box from '../box';
import Text from '../../misc/text';
import {
  Story,
  Meta,
  Preview,
  Props,
  Link
} from "@storybook/addon-docs/blocks";

<Meta title='Layout/Dynamic Panel' component={DynamicTabs} />

# Dynamic Panels

<hr />

Dynamic Panels separate the Application into specific tabs for better navigation and organizing, dynamically building contexts which holds any component in them. They can be dynamically added, removed and updated.

<Preview>
  <Story name="Dynamic Sample">
    <DynamicTabsSample />
  </Story>
</Preview>

<Props of={DynamicTabs} />