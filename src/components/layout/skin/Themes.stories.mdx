import { Story, Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  LightTheme,
  DarkTheme,
} from './template';

<Meta title="Layout/Theme" />

# Themes
<hr />

There are **2** pre defined themes you can use. **``Light``** and **`Dark`**. Their Keys
and values are as follows:

Once you have access to the current theme object you may access the color hex value like so:

```jsx
const primaryColor = theme.colors.primary_100;
```

### Light Theme
<Preview withSource="none">
  <Story name="light">
    <LightTheme />
  </Story>
</Preview>

### Dark Theme
<Preview withSource="none">
  <Story name="dark">
    <DarkTheme />
  </Story>
</Preview>
